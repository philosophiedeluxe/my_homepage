# Server_CSS v11 clean hover spacing

Basis: v10 clean hover.

Änderungen:

- `fixes.css` bleibt entfernt.
- Zentraler Single-Aufruf bleibt erhalten.
- Hover-/Aktiv-Flächen der Tabs werden optisch um ca. 10% niedriger dargestellt.
- Das Touch-/Klick-Ziel der Tabs bleibt unverändert.
- Der Abstand über Abschnittstiteln wie `Meine Medien` wurde leicht erhöht.
- Keine Änderungen an Card-Grids, Posterbildern, Detailseiten-Positionierung oder dynamischer Skalierung.

Jellyfin-Aufruf:

```css
@import url('https://philosophiedeluxe.github.io/my_homepage/Server_CSS/jellyfin_custom.css?v=14');
```
